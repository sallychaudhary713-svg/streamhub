{
  "name": "movie-site",
  "private": true,
  "version": "1.0.0"
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "vite preview --host 0.0.0.0 --port $PORT",
  }
  "devDependencies": {
    "vite": "^5.4.0"
  }
}    
    
    
    
    
