{
  "name": "movie-site",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "npx vite build",
    "start": "npx vite preview --host 0.0.0.0 --port $PORT"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
    
    
    
    
